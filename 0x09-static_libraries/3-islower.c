#include "main.h"
/**
 * _islower - function thatchecks for lowercase character
 * @c: the character to check
 * Return: return 1, otherwise 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
