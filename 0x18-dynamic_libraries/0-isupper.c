#include "main.h"
/**
* _isupper - A program that is checking capital letters
* @c: character to be checked
* Return: it is 1 if its upper case,0 when otherwise
*/

int _isupper(int c)
{
if  (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
